from django.forms import ModelForm
from stimulustracker.links.models import Link

class LinkForm(ModelForm):
    class Meta:
        model = Link
        fields = ['link_title', 'link_url', 'link_source', 'link_source_url']
